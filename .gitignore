# env files (root-level, e.g. docker-compose credentials)
.env
.env.local
.env.*.local
!.env.example

# OS/editor noise
.DS_Store

# apps/ and packages/ each have their own .gitignore for
# node_modules, build output, coverage, etc.
